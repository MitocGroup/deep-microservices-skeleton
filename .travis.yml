language: node_js
sudo: false
node_js:
  - '4.3'

cache:
  directories:
    - $(npm root -g)
    - node_modules

branches:
  only:
    - master
    - dev
    - stage
    - test

before_install:
  - npm run prepare

after_success:
  - npm run coverage
